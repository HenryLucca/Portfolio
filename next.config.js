/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // no need to setup `i18n` config as i'm handling it manually
  // i18n: {
  //   defaultLocale: 'en',
  //   locales: ['en', 'pt-BR'],
  // }
  
}

module.exports = nextConfig
